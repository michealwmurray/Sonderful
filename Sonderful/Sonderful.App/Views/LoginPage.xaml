<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Sonderful.App.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Sonderful.App.ViewModels"
    x:DataType="vm:LoginViewModel"
    Title=""
    Shell.NavBarIsVisible="False"
    BackgroundColor="{StaticResource PageBg}">

    <Grid>
        <!-- Brand header with rounded bottom -->
        <Border
            VerticalOptions="Start"
            BackgroundColor="{StaticResource Brand}"
            Stroke="Transparent"
            StrokeThickness="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="0,0,40,40" />
            </Border.StrokeShape>
            <VerticalStackLayout Padding="32,72,32,56" Spacing="6" HorizontalOptions="Center">
                <Label
                    Text="✦"
                    FontFamily="JakartaExtraBold"
                    FontSize="44"
                    TextColor="White"
                    HorizontalOptions="Center" />
                <Label
                    Text="Sonderful"
                    FontFamily="JakartaExtraBold"
                    FontSize="36"
                    TextColor="White"
                    HorizontalOptions="Center" />
                <Label
                    Text="Discover plans around you"
                    FontFamily="JakartaRegular"
                    FontSize="15"
                    TextColor="#CCFFFFFF"
                    HorizontalOptions="Center" />
            </VerticalStackLayout>
        </Border>

        <!-- Form card scrolls over the header -->
        <ScrollView Margin="0,220,0,0">
            <VerticalStackLayout Padding="24,0,24,40" Spacing="0" HorizontalOptions="Center" MinimumWidthRequest="{OnPlatform WinUI=400, Android=0}">

                <!-- White card -->
                <Border
                    BackgroundColor="{StaticResource Surface}"
                    Stroke="Transparent"
                    StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="24" />
                    </Border.StrokeShape>
                    <Border.Shadow>
                        <Shadow Brush="{StaticResource BlackBrush}" Opacity="0.08" Offset="0,4" Radius="20" />
                    </Border.Shadow>

                    <VerticalStackLayout Padding="28,28,28,32" Spacing="16">

                        <Label
                            Text="Welcome back"
                            FontFamily="JakartaBold"
                            FontSize="22"
                            TextColor="{StaticResource Ink}" />

                        <Border Style="{StaticResource InputFieldBorder}">
                            <Entry
                                Placeholder="Email or username"
                                Text="{Binding Identifier}"
                                HeightRequest="52"
                                BackgroundColor="Transparent" />
                        </Border>

                        <!-- Password field -->
                        <Border Style="{StaticResource InputFieldBorder}">
                            <Entry
                                Placeholder="Password"
                                Text="{Binding Password}"
                                IsPassword="True"
                                HeightRequest="52"
                                BackgroundColor="Transparent" />
                        </Border>

                        <Button
                            Text="Log In"
                            Command="{Binding LoginCommand}"
                            Margin="0,8,0,0" />

                    </VerticalStackLayout>
                </Border>

                <!-- Sign up link -->
                <Button
                    Text="Don't have an account? Sign up"
                    Command="{Binding GoToRegisterCommand}"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource Brand}"
                    FontFamily="JakartaMedium"
                    FontSize="14"
                    HeightRequest="48"
                    Margin="0,12,0,0" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
